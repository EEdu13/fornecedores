[phases.setup]
nixPkgs = ['python312', 'gcc', 'unixODBC', 'unixODBCDrivers']

[phases.install]
cmds = ['python -m venv --copies /opt/venv', '. /opt/venv/bin/activate && pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase completed"']

[start]
cmd = 'python photo_server.py'

[variables]
PORT = '8000'
HOST = '0.0.0.0'